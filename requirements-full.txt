# Full image extras: actually install the third-party guardrail engines so
# they run for real (not just the graceful-fallback adapters).
-r requirements.txt

# Microsoft Presidio — NER-backed PII detection/anonymization (offline).
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# NVIDIA NeMo Guardrails — input rails / self-check.
# Its self-check rail calls an LLM, so it fully activates when a Claude
# credential (ANTHROPIC_API_KEY) is available; otherwise the adapter degrades.
nemoguardrails==0.11.0
