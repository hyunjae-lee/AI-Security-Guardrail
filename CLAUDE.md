# CLAUDE.md — 디지털 국경: AI 보안 가드레일 설명 사이트

> 저장소 루트에 있고 git 으로 함께 관리한다 — 어느 컴퓨터에서 clone 하든 이 문서가 따라온다.
> 최종 갱신 2026-09-13.

## 저장소 컨텍스트
저장소: `AI-Security-Guardrail/` (origin: github.com/hyunjae-lee/AI-Security-Guardrail).
두 덩어리가 한 컨테이너로 배포된다.
- `app/` — FastAPI 가드레일 엔진 + 실시간 데모(SSE). `attacks/`, `tests/`, `scripts/`, Docker 구성.
- `web/` — 「디지털 국경」 스크롤텔링 설명 사이트 (Vite + vanilla JS + 인라인 SVG).

### 작업 규칙 (현행)
- **main이 작업 브랜치다.** 초기 사이트 작업은 `feature/digital-border`에서 하고 PR #1로 머지했으나, 이후 작업은 main 직접 푸시로 진행 중이다.
- **main 푸시 = 실서비스 재배포.** push → CI(`ci.yml`) → 성공 시 CD(`cd.yml`, self-hosted `guardrail-host` 러너)가 컨테이너를 재빌드·재기동한다. 푸시 전에 로컬에서 `pytest`와 `npm run build`가 통과하는지 반드시 확인할 것.
- 규모가 크거나 되돌릴 여지가 필요한 변경은 브랜치 + PR로. 소규모 카피·도면 수정은 main 직행이 현재 관행.
- `app/` 수정은 이제 허용된다 (설명 사이트 연동 때문에 이미 수정됨: `config.py`, `main.py`, `static/`). 단 **가드레일 엔진 로직(`app/guardrail/`)은 사이트 작업 중에 건드리지 않는다** — 사이트 문구가 엔진을 따라가야지 그 반대가 아니다.
- 포트: 데모 8088, Vite dev 5173 (겹치지 않게 유지).

## 프로젝트 한 줄 정의
KAIST 구성원의 AI 질의가 "국경(게이트웨이)"을 넘나드는 과정을, 유튜브 '신비한 건축사전' 스타일의 건축 단면(컷어웨이) 스크롤텔링으로 설명하는 단일 페이지 웹사이트. 비유는 **국제공항**으로 확정.

## 목적·타깃
- 용도: 연구 최종발표 시연 + 교내 구성원 교육 자료.
- 타깃 독자: 비전문가 교직원 기준. 본문은 비유어로 쓰고, 실제 기술 용어는 각 섹션의 접이식 "실제로는" 패널에만 병기한다.
- 언어: 한국어 단독 (모든 문자열은 `content/strings.js`에 상수로 분리 — 추후 i18n 가능하게).

## 배포 (동작 중)
| 항목 | 값 |
|---|---|
| 공개 주소 | `https://guardrail.kaist.ac.kr` (KAIST 내부망에서만 접근 가능) |
| 실시간 데모 | `/` |
| 설명 사이트 | `/explain/` |
| 호스트 | 143.248.4.101 : 8088 (직결도 열려 있음 — CD 헬스체크가 사용) |
| 앞단 | SSL Manager Caddy가 443 → 8088 리버스 프록시. 설정은 이 저장소가 아니라 호스트의 `/home/kaistcert/workdir/Caddyfile` |
| 인증서 | 기존 `*.kaist.ac.kr` 와일드카드 (**2026-12-17 만료** — 갱신 필요) |

번들 배선: `Dockerfile.full`의 `node:22-alpine` 스테이지가 `VITE_BASE=/explain/ npm run build` → `COPY --from=web /web/dist ./web/dist`. `app/config.py`의 `explain_dir`(`web/dist`)이 존재할 때만 `app/main.py`가 `/explain`에 `StaticFiles(html=True)`로 마운트한다. 호스트에 Node 불필요.

로컬에서 배포와 동일하게 확인하려면:
```bash
cd web && VITE_BASE=/explain/ npm run build   # /explain/assets/... 경로로 빌드
# 순수 사이트 개발은  cd web && npm run dev   (5173, base '/')
```
자세한 내용은 저장소의 `DEPLOY.md`.

## 이어서 작업하기 (다른 컴퓨터에서)

```bash
git clone https://github.com/hyunjae-lee/AI-Security-Guardrail.git
cd AI-Security-Guardrail/web
npm ci                 # package-lock 그대로 설치 (npm install 말고)
npm run dev            # http://localhost:5173
```

필요한 것은 **Node 20 이상**뿐이다. 사이트는 정적 번들이라 파이썬·도커·백엔드 없이 뜬다.
`app/` 쪽 테스트까지 돌리려면 저장소 루트에서 `pip install -r requirements-dev.txt && pytest`.

- **`localhost` 로 안 열릴 때**: `http://127.0.0.1:5173` 을 쓴다. `localhost` 가 IPv6(`::1`)
  로 먼저 풀리는 컴퓨터에서는 Vite 가 IPv4 에만 물려 있어 연결이 끊긴다.
  (`vite.config.js` 의 `server.host` 를 건드리면 LAN 에 열리므로 기본값을 그대로 둔다.)
- **캡처를 다시 찍을 때**: `tools/capture-sources.mjs` 안의 Chrome 실행 경로(`CHROME`)가
  이 맥 기준이다. 다른 컴퓨터에서는 그 줄만 자기 경로로 바꾼다.
- 작업 전에 `git pull` 하고, 커밋 전에 아래 「검증 도구」를 돌린다.

## 비유 매핑 (고정 — 임의 변경 금지)
| 실제 시스템 | 공항 비유 |
|---|---|
| KAIST 네트워크 | 우리 영토(캠퍼스) |
| 단말(폰·태블릿·PC·노트북)의 질의 | 캠퍼스 곳곳에서 공항으로 모이는 여행 가방 |
| 외부 생성형 AI (ChatGPT·Claude 등) | 국경 밖 — 지구본으로 표현 |
| 가드레일 게이트웨이 | 국제공항 터미널 — 나가려면 반드시 여기를 거침 |
| 질의 필터 (1차, outbound) | 출국장 보안검색 — X-ray로 반출 금지품 검사 |
| 개인정보 마스킹 (MASK) | 금지 물건만 빼고 출국 (가방째 압수 아님) |
| 주민번호 등 L4 | 전략물자 — 어떤 경우에도 반출 금지 (즉시 차단) |
| 프롬프트 인젝션 | 위조 여권·가방 속 밀수품 → 탑승 거부 (BLOCK) |
| 외부 AI 처리 | 국제선 비행 + 해외 현지 처리 |
| 답변 필터 (2차, inbound) | 입국장 세관·검역 — 숨어 들어온 해충(유출 링크·카나리아 토큰·재노출 PII) 차단 |
| 사용자 인증 + CLR-0~4 | 여권 + 비자 등급 |
| 데이터 등급 L0~L4 | 물품 등급표 (L4 = 전략물자) |
| 감사 로그 (원문 미저장) | 출입국 기록부 — 품목·판정만 기록, 가방 내용물 원본은 안 남김 |
| 부서별 데이터 등급 분류 책임 | 품목표는 각 부서가 쓴다 (SCENE 09) |
| (확장, 미구현) 교내 로컬 LLM | 국내선 — 국경을 넘지 않는 노선 |

## 페이지 구성 (섹션 10개, 스크롤 순서 고정)
초기 기획은 7개였으나 "왜 필요한가 → 무슨 근거로 → 누구 책임인가"를 세우기 위해 3개(02·03·09)를 증설했다.

| # | id | 라벨 | 하는 일 |
|---|---|---|---|
| 01 | `intro` | 인트로 | "무엇을 입력하든, 막는 것이 없습니다" + **카드뉴스 8장**(제목 바로 아래) — 통제 없는 현재 상태 |
| 02 | `why` | 왜 검사대인가 | "지금은 아무도 가방을 열어 보지 않습니다" — 문제 제기 |
| 03 | `basis` | 설계 근거 | N2SF 가이드라인 **원문 인용** + 표지 이미지 |
| 04 | `overview` | 조감도 | 캠퍼스 ↔ 지구본, 유일한 통로인 터미널. **"터미널 = 가드레일" 등식을 여기서 못박는다** |
| 05 | `departures` | 출국층 단면 | 여권·비자 확인 → X-ray → 게이트 3갈래. 전략물자·위조 여권은 게이트를 건너뛰고 즉시 거부 |
| 06 | `runway` | 활주로 | 국경 밖은 우리 관제 밖 (톤을 어둡게) |
| 07 | `arrivals` | 입국층 단면 | 돌아온 가방의 검역 — "나갈 때 깨끗했어도 돌아올 때 깨끗하다는 보장은 없다" |
| 08 | `records` | 기록실 | 판정 스탬프만 찍히고 내용 칸은 비어 있음 |
| 09 | `shared` | 각 부서의 몫 | "장비는 우리가 만듭니다. 품목표는 우리가 못 씁니다" — 등급 분류 책임 |
| 10 | `outro` | 아웃트로 | 터미널이 지표 아래로 — "안 보일 뿐 그대로 돕니다" + `/`(데모) 링크 |

부가 블록: `headlines`(SCENE 01 카드뉴스), `latency`(실측 지연 표 + 측정 방법 패널), `legend`(그림 읽는 법), `cases`(사례 카드), `summary`(정리), 전 장면 `reveals`("실제로는" 접이식 패널).

### SCENE 01 카드뉴스 — 원문 화면 캡처 8장
제목과 본문 리드 **사이**에 끼어 있다(가로 스크롤 스냅). **머리말·안내문 없이 곧바로 카드로 넘어간다** — 인트로의 목적은 설명이 아니라 공감이고, 설명을 앞에 두면 그 자리에서 읽기를 멈춘다. 남는 UI 는 위치 표시(n/8)와 좌우 버튼뿐.

구성은 위협의 종류로 묶는다:
| # | 카드 | 무엇을 말하나 |
|---|---|---|
| 1~4 | 실제 사고 | 붙여넣기(삼성) · 공유 버튼(구글 색인) · 클릭 0회(EchoLeak) · 화상회의 사칭(딥페이크) |
| 5~6 | 조용히 바뀐 기본값 | 약관 개정(Anthropic) · 법원의 기록 요구(OpenAI) |
| 7~8 | 숫자로 보면 | 다크웹의 AI 계정(IBM X-Force) · 파악 격차 78% vs 23%(Okta) |

규칙:
- **주장 금지, 확인된 사실만.** 카드마다 원문 화면 캡처 + 출처 + 날짜.
- **캡처와 문구가 어긋나면 안 된다.** 캡처에 보이는 날짜·제목·제품명이 카드의 date·source·본문과 같아야 한다. 캡처가 뒷받침하지 못하는 사실은 본문에서 별도 날짜와 함께 밝힌다.
- **제도·법령은 여기 넣지 않는다.** N2SF·AI 기본법 같은 제도 근거는 SCENE 03 이 정면으로 다룬다. 인트로는 위협에만 집중해야 공감이 흩어지지 않는다.
- 마지막 `closing` 이 "대단한 해킹이 하나도 없다" 로 SCENE 02 에 넘긴다.

**캡처 팝업**: 카드 그림을 누르면 `<dialog>` 로 원문 화면을 크게 띄운다. 왼쪽은 캡처 + 봐야 할 곳을 짚는 네모(바깥을 어둡게 눌러 스포트라이트), 오른쪽은 같은 본문에 형광펜만 얹은 것. 카드마다 strings 에 세 가지를 함께 둔다:
- `mark: { x, y, w, h }` — 캡처에서 짚을 영역, **퍼센트**. 캡처를 다시 찍으면 이 값도 반드시 다시 맞춘다. 눈대중으로 넣지 말고, 캡처 위에 네모를 그려 확인한 뒤 값을 정한다(제목 한 덩어리를 여유 없이 감싸는 정도가 맞다).
- `note` — "여기를 보세요" 뒤에 붙는 한 줄. 그 영역이 무엇인지 설명한다.
- `detail` — 팝업 본문. 카드의 `body` 보다 길고 구체적으로 쓴다. **기업명·기관명·국가명·CVE 번호는 줄이지 말고 그대로 적는다** — 근거로 보이려면 고유명사가 있어야 한다. 강조는 인라인 태그로 넣는다:
  - `<b>` = 고유명사·기관명 (흰 굵은 글씨)
  - `<mark class="hl">` = 그 카드의 핵심 사실 (카드 색 + 굵게)
  두 층을 섞지 말 것 — 이름은 `<b>`, 사실은 `<mark>`.

## 비주얼 디자인 시스템

두 층으로 나뉜다. **지면(page)은 Apple 디자인 언어**, **도면(plate)은 기존 다크 아이소메트릭**.
지면이 물러나야 도면이 말한다 — 애플이 제품 사진을 다루는 방식 그대로다.
실제 값은 `styles/tokens.css`가 기준이고, 도면 안쪽 규칙은 `styles/plate.css`에 따로 있다.

### 1층 — 지면 (Apple)
- **강조색은 하나다.** Action Blue `#0066cc`. 누를 수 있는 것은 전부 이 색이고, 두 번째 브랜드 색은 없다.
  포커스 링 `#0071e3`, 어두운 타일 위 링크 `#2997ff`.
- **표면**: 흰 캔버스 `#ffffff` · 파치먼트 `#f5f5f7` · 어두운 타일 `#272729`/`#252527`.
  장면은 가장자리까지 꽉 찬 **타일**이고 밝은 타일과 어두운 타일이 번갈아 온다.
  **색이 바뀌는 것 자체가 구분선이다** — 장면 사이에 선을 긋지 않는다.
  톤 배정은 `scenes/_tone.js`의 `TILE` 하나뿐이다 — `main.js`(마크업)와
  `tools/render-scenes.mjs`(미리보기)가 같이 읽으므로 한쪽만 고치면 어긋난다.
  어두운 타일은 두 장뿐이고 둘 다 내용이 요구한다:
  06 활주로(국경 밖), 10 아웃트로(지표 아래).
- **글자**: 잉크 `#1d1d1f`, 보조 `#333333`/`#7a7a7a`. 본문은 **17px**(16px 아님) / 굵기 400.
  굵기 사다리는 **300 / 400 / 600 / 700 — 500은 쓰지 않는다**. 제목은 600.
- **타이포**: `-apple-system → Inter → Noto Sans KR`. 맥·iOS에서는 진짜 SF Pro로 풀리고,
  다른 플랫폼은 Inter가 받는다. 한글은 Inter에 글리프가 없어 자연히 Noto Sans KR로 떨어진다.
  큰 글씨일수록 자간을 좁힌다("Apple tight"). 12px 이하에는 쓰지 않는다.
- **그림자는 시스템 전체에 딱 하나**: `3px 5px 30px rgba(0,0,0,0.22)`.
  도면 판과 N2SF 표지처럼 **그림이 바닥에 앉을 때만** 쓴다. 카드·버튼·글자에는 쓰지 않는다.
- **모서리**: 유틸리티 8px · 진주빛 캡슐 11px · 카드/도면 판 18px · 알약 9999px. 그 사이 값은 쓰지 않는다.
- **장식 금지**: 그러데이션·도면 트림 마크·장식 테두리 없음. 분위기는 그림이 만든다.
- 누름 반응은 전 화면 공통으로 `transform: scale(0.95)` 하나.
- 상단 바는 서리 유리(파치먼트 80% + `saturate(180%) blur(20px)`).

### 2층 — 도면 (밝은 아이소메트릭)
- 스타일: 신비한 건축사전 방식 — 아이소메트릭/단면 컷어웨이, 부품별 라벨 + 얇은 지시선,
  스크롤에 따라 구조물이 순차 조립·해부되는 연출.
- **판(plate)을 깔지 않는다.** 도면은 타일 위에 그대로 놓이고, 바닥에 닿아 있다는 신호는
  도면 안의 `.iso-shadow`(검정 13%)가 낸다. 예외는 어두운 타일 — 거기서는 도면을
  파치먼트 판 위에 올리고 시스템의 유일한 그림자를 준다(아래).
- 면 색은 애플 계열 중성 회색(`#fbfbfd` → `#c7c7cc`). 광원은 여전히 좌상단 —
  윗면이 가장 밝고, 좌면(+y) 중간, 우면(+x) 가장 어둡다.
- 영역 색: 캠퍼스 = 찬 청회색, 검사장 = 청록(답이 되는 구조물이라 가장 밝게),
  국경 밖 = 보랏빛. 세 영역이 색상환에서 떨어져 있어야 축소 화면에서도 갈린다.
- **어두운 타일의 도면은 밝은 판 위에 올린다** (`.scene--dark .scene__stage`).
  도면이 밝아서, 어두운 타일에 그대로 놓으면 라벨이 빈 자리에 얹히느냐 도면 위에
  얹히느냐에 따라 읽히는 색이 달라진다. 판을 깔면 판 안은 언제나 밝고 라벨 색이
  흔들리지 않는다 — 어두운 섹션에 제품 사진 한 장을 놓는 것과 같다.

#### 색을 어디에 적는가 (중요)
| 칠하는 방법 | 어디에 정의 | 비고 |
|---|---|---|
| CSS 클래스 (`.f-top`, `.home-l`, `.gate-edge` …) | `styles/tokens.css` + `styles/plate.css` | 3면 명도가 서로 맞물려 있으니 한 값만 따로 고치지 말 것 |
| SVG 어트리뷰트 (`fill="…"`) | `scenes/_palette.js` 의 `C` | **SVG 프레젠테이션 어트리뷰트는 `var()` 를 못 받는다** — `fill="var(--x)"` 는 브라우저에서 늘 검정으로 떨어진다 |
| 인라인 스타일 (`style="fill:…"`) | `var(--…)` 사용 가능 | 타일 톤을 따라가야 하는 글자는 여기로 |

**시맨틱 색은 두 벌이다.** 물체색(면·선)과 잉크(글자)를 섞지 말 것.
| 뜻 | 물체색 (면·선) | 잉크 (흰 판독 화면 위 글자) |
|---|---|---|
| 질의 · 가방 | `#D98F1F` | `#8A5A06` |
| 설비 · 직원 | `#12A184` | `#0D7A63` |
| 차단 · 경보 | `#D9453A` | `#B3251A` |
| 허용 | `#5DA83A` | `#3F7A24` |

물체색은 흰 타일(#fff)과 어두운 타일(#272729) 양쪽에서 3:1 을 넘기는 중간 명도,
잉크는 흰 바탕에서 4.5:1 을 넘긴다. 면 색을 그대로 글자에 쓰면 흰 판 위에서 대비가
무너진다 — `node web/tools/check-contrast.mjs` 가 이것을 잡는다.

**도면 색을 고쳤으면 반드시 눈으로 볼 것.** 좌표 검사도 대비 검사도 "바탕과 같아져
사라진 것" 을 다 잡지는 못한다. 이 호스트에는 브라우저가 없으므로
`node web/tools/render-scenes.mjs` 로 PNG 를 구워 확인한다 (장면 id 를 주면 그것만:
`node web/tools/render-scenes.mjs runway outro`). 이 도구는 tokens.css 의 변수를 미리
치환하고 `_tone.js` 의 바탕색을 깔아 화면과 같은 그림을 만든다.
애니메이션은 나오지 않는다 — '동작 줄이기' 로 보는 정지 화면과 같다.

- 도면 안의 판(판독 화면·배지·기록 카드)은 타일 색과 무관하게 언제나 밝다 —
  종이 한 장이 놓인 것으로 읽혀야 한다. 반드시 테두리(`C.panelLine` 또는 시맨틱 색)를 줄 것.
- **도면 글자는 `--font-plate`(Noto Sans KR)로 고정**하고 자간을 `normal`로 되돌린다.
  `_svg.js`의 `advance()` 글자 폭 표가 이 서체 기준이고, 그 값으로 형광 상자 폭을 계산한다.
- 일러스트는 전부 인라인 SVG 직접 제작(아이콘 폰트·이모지·스톡 이미지 금지).
- **예외는 「근거 화면」 두 종류뿐**이다. 우리가 그린 그림이 아니라 남의 원문을 보이는 자리:
  - `web/src/assets/n2sf-cover.webp` — N2SF 가이드라인 실물 표지 (SCENE 03).
  - `web/src/assets/captures/*.jpg` — SCENE 01 카드뉴스의 원문 화면 캡처 8종.
  둘 다 출처·날짜를 화면에 함께 표기하고, 캡처에는 「원문 화면 캡처」 배지를 붙인다.
  장식용 사진을 들여오는 통로가 아니다 — 인용 근거가 아니면 여전히 금지.
- 사람은 단순 실루엣(원 머리 + 라운드 몸통), 사물은 플랫 도형 + 얇은 스트로크.
- 각 섹션 하단에 한 줄 캡션. 캡션만 읽어도 전체가 이해되어야 한다.

### 3층 — 모션 (Apple)
연출은 셋뿐이다. 더 넣지 말 것 — 지면이 물러나야 도면이 말한다.
이징은 전부 `power3.out`(애플의 `cubic-bezier(.28,.11,.32,1)` 에 가장 가깝고 GSAP 무료 범위).
1. **올라오며 나타나기** — 28px 아래에서, 형제끼리 0.08s 씩 늦게.
2. **제목은 줄 단위** — 줄마다 창(overflow:hidden) 안에서 밀려 올라온다. 마크업이 두 겹인 이유.
3. **도면은 천천히 흐른다** — 타일보다 느리게(±22px scrub). 834px 이하에서는 끈다.

`prefers-reduced-motion` 은 그대로 존중한다 — 모션이 꺼지면 위 셋을 아예 붙이지 않아
**완성된 정지 화면**이 남는다. 설명이 통째로 사라지면 안 된다(f2419ea 회귀).

## 기술 스택·제약
- Vite 7 + vanilla JS + 인라인 SVG. 프레임워크 없음.
- 애니메이션: GSAP + ScrollTrigger + MotionPathPlugin (무료 공개 기능만). **번들에 들어가는 의존성은 GSAP 하나뿐** — 추가 시 사유 먼저 제시.
- devDependency 는 둘: `vite`(빌드), `@resvg/resvg-js`(도면 미리보기).
  후자는 이 호스트에 브라우저가 없어 도면을 눈으로 확인할 방법이 없기 때문에 들였다.
  `tools/render-scenes.mjs` 만 쓰고 사이트 번들에는 들어가지 않는다.
  프리빌트 바이너리라 `node:22-alpine`(musl)에서도 `npm ci` 로 그대로 깔린다 — 확인함.
- three.js 등 3D 금지 — 2.5D 아이소메트릭 SVG.
- 사이트에서 API 호출 없음(정적 번들). 실동작은 같은 호스트의 `/` 데모가 담당.
- 성능: 60fps 목표. 무거운 필터/블러 남용 금지.
- 접근성: `svgWrap()`이 모든 SVG에 `role="img"` + `<title>`/`<desc>` + `aria-labelledby`를 강제한다 — SVG를 새로 만들 때 반드시 이 헬퍼를 쓸 것. 도트 앵커 내비로 키보드 이동 가능.
- `prefers-reduced-motion`: 애니메이션을 끄되 **완성된 정지 화면**을 보여 준다. 설명이 통째로 사라지면 안 된다(f2419ea에서 고친 회귀).
- 해상도: 데스크톱(1440~1920) 우선. 모바일은 깨지지 않는 수준.

## 검증 도구 (커밋 전 실행)
```bash
node web/tools/check-physics.mjs      # 아이소메트릭 도면 물리 오류 검사 — 10장면 전부 ✓ 여야 함
node web/tools/check-contrast.mjs     # 도면 색 대비 — 바탕과 같아져 사라진 것이 없는지
node web/tools/render-scenes.mjs      # 도면을 PNG 로 구워 눈으로 확인 (web/tools/.out/)
cd web && npm run build               # 빌드 통과 확인
python web/tools/bench-latency.py     # 지연 실측 (표에 쓰는 숫자를 여기서 얻는다)
node web/tools/capture-sources.mjs    # 카드뉴스 근거 화면 다시 찍기 (출처가 바뀌었을 때만)
pytest                                # app/ 을 건드렸다면
```

캡처를 다시 찍었으면 커밋 전에 축소·변환한다 — 원본 PNG 를 그대로 싣지 않는다:
```bash
cd web/src/assets/captures && for f in *.png; do
  sips -Z 800 "$f" --out "${f%.png}.jpg" -s format jpeg -s formatOptions 72 >/dev/null; done && rm -f *.png
```

## 정확성 원칙 (내용 검증 기준)
"실제로는" 패널과 본문의 사실 진술은 아래 범위를 벗어나지 말 것. **엔진 코드가 언제나 기준이다** (`app/guardrail/engine.py`).

- 처리 흐름: 질의 수신(원문 영구 저장 금지) → 탐지·분류 → 정책 결정 → 권한 검색(RAG 검색 전 CLR 필터) → 출력 재검사·감사.
- **판정은 4갈래**: ALLOW / SANITIZE(마스킹) / FLAG(전달하되 감사 기록에 표시) / BLOCK.
  - 그림(SCENE 05)은 게이트를 **3갈래로 단순화**해 그린다. FLAG는 "실제로는" 패널에서만 언급 — 의도된 단순화이므로 도면을 4갈래로 고치지 말 것.
- **임계치는 프로파일별로 다르다** (`PROFILES`). 기본값은 `balanced`:
  | 프로파일 | sanitize_at | flag_at | block_at |
  |---|---|---|---|
  | 엄격 strict | 15 | 30 | 50 |
  | **균형 balanced (기본)** | **20** | **45** | **70** |
  | 완화 permissive | 35 | 65 | 88 |
  - 숫자를 쓸 때는 반드시 어느 프로파일 기준인지 함께 밝힐 것.
- **무조건 차단은 3종** (`_HARD_BLOCK`: `harmful.csam`, `harmful.self_harm`, `harmful.weapons`) — 점수와 무관하게 즉시 BLOCK. (완화 프로파일은 2종)
- 마스킹만으로 해결되는 유형(`pii.`, `obfuscation.`)은 임계치를 넘어도 차단하지 않고 치환 후 전달한다.
- 점수 산정 방식은 두 가지: `worst_decay`(**런타임 기본값** — 최악 항목이 바닥을 정하고 추가 항목은 감쇠 가산)와 `sum`(중간발표 프레임워크의 단순 합산). 0~100 범위.
- 등급: 데이터 L0~L4(L4 절대 반출 금지), 권한 CLR-0~4, "CLR ≥ L일 때만 접근".
- 감사 로그는 원문 없이 탐지 유형·룰 ID·판정만 기록.
- 마스킹은 삭제가 아닌 변환(부분 마스킹·토큰화·일반화).
- 출력 검사가 잡는 것: PII 재노출, 마스킹 풀림, 유출 링크, 카나리아 토큰.
- **수치 표기 규칙**: 추정치·희망치는 금지. 지연 수치는 `web/tools/bench-latency.py`로 **실측한 값만** 쓰고, 반드시 측정 방법 패널(`latency.method`)을 함께 노출한다. 정확도 %처럼 아직 측정되지 않은 지표는 여전히 표기 금지.

## 폴더 구조
```
AI-Security-Guardrail/
  app/                        ← FastAPI 엔진 + 데모. config.py 의 explain_dir, main.py 의 /explain 마운트
  tests/                      ← pytest (테스트 함수 67개)
  attacks/  scripts/  docs/
  Dockerfile.full             ← node 스테이지에서 web/ 번들 빌드 → 이미지에 포함
  DEPLOY.md                   ← 호스트 제약·도메인·Caddy·CD 등록 절차
  .github/workflows/          ← ci.yml, cd.yml
  web/
    index.html
    vite.config.js            ← base = process.env.VITE_BASE || '/'
    tools/check-physics.mjs   ← 도면 물리 검사
    tools/check-contrast.mjs  ← 도면 색 대비 검사
    tools/render-scenes.mjs   ← 도면을 PNG 로 구움 (브라우저 없는 호스트의 눈)
    tools/bench-latency.py    ← 지연 실측
    src/
      main.js                 ← 10개 장면 등록 + ScrollTrigger 초기화 + 내비·진행바
      assets/n2sf-cover.webp
      content/strings.js      ← 모든 한국어 카피 (유일한 카피 소스)
      styles/tokens.css       ← 색·타이포 변수 (지면=Apple / 도면=다크 두 층)
      styles/main.css         ← 지면(Apple 디자인 언어)
      styles/plate.css        ← 도면 SVG 안쪽 (값 변경 금지)
      scenes/_svg.js          ← svgWrap() 등 공용 헬퍼 (SVG는 반드시 여기 경유)
      scenes/_palette.js      ← SVG 어트리뷰트로 칠하는 색 (var() 를 못 쓰는 자리)
      scenes/_tone.js         ← 장면별 타일 톤 (main.js·render-scenes 가 같이 읽음)
      scenes/_iso.js          ← 아이소메트릭 투영 유틸
      scenes/_places.js       ← 반복 사물(가방·사람·설비) 빌더
      scenes/scene-*.js       ← 장면별 SVG 빌더 + 애니메이션
```

## 현재 상태
M1~M5 및 2차 단계(`/explain` 서빙) 모두 완료. 10개 장면 전부 인라인 SVG + 애니메이션 + "실제로는" 패널을 갖췄고, CI/CD로 실호스트에 배포되어 동작 중이다. 남은 것은 카피 교열과 발표 대비 미세 조정 수준.

### 알려진 이슈
- `strings.js`가 위험 점수를 **"합산"**이라 서술하지만(3곳), 런타임 기본 산정 방식은 `worst_decay`다. 문구를 고치든 데모 기본값을 `sum`으로 두든 한쪽으로 정리 필요.
- 와일드카드 인증서 2026-12-17 만료.

## Do / Don't
- Do: 의미 단위로 커밋(현재 커밋 메시지는 "무엇을 왜 바꿨는지"를 한국어 한 문장으로 쓰는 스타일). 완료 시 실행 방법과 확인 포인트를 짧게 보고.
- Do: 카피는 `strings.js`에만. SVG/JS에 하드코딩 금지.
- Do: 푸시 전에 물리 검사 + 빌드 통과 확인 (main 푸시는 곧바로 실배포다).
- Don't: 라이브러리 추가 남발, 스톡 이미지, 측정 안 된 수치 표기, 비유 매핑 임의 변경, 사이트 문구에 맞추려고 엔진 상수 수정.
