/**
 * 도면 색 — SVG 안쪽에서만 쓰는 값.
 *
 * SVG 의 `fill="…"` 은 프레젠테이션 어트리뷰트라 CSS 변수(var())를 받지 못한다.
 * 그래서 면·괘선처럼 클래스로 칠할 수 있는 것은 styles/plate.css 가 맡고,
 * 어트리뷰트로 직접 칠해야 하는 것만 여기 상수로 모은다.
 * 장면 파일에 헥사를 직접 적지 말 것 — 여기서만 정의한다.
 *
 * 두 벌인 이유
 * ------------
 * 도면은 밝은 타일(흰·파치먼트)과 어두운 타일(#272729) 양쪽에 올라간다.
 * 그래서 색을 두 층으로 나눈다.
 *
 *   1) 물체색 (bag/gear/block/allow) — 도형의 면과 선.
 *      두 바탕 모두에서 3:1 이상으로 읽히는 중간 명도로 잡았다.
 *   2) 글자색 (…Ink) — 도면 안 흰 판독 화면 위에 얹히는 글자.
 *      흰 바탕에서 4.5:1 을 넘겨야 하므로 한 단계 더 눌렀다.
 *
 * 판독 화면의 글자에는 반드시 Ink 쪽을 쓴다. 물체색을 글자로 쓰면
 * 흰 판 위에서 대비가 무너진다.
 */

export const C = {
  /* --- 물체색 ---------------------------------------------------------- */
  bag: '#d98f1f', // 질의 · 여행 가방
  bagLit: '#f2b457', // 가방 윗면
  bagDeep: '#b07213', // 가방 우면 · 경로 화살
  gear: '#12a184', // 가드레일 설비 · 직원
  block: '#d9453a', // 차단 · 경보
  allow: '#5da83a', // 허용

  /* --- 글자색 (흰 판독 화면 위) ------------------------------------------ */
  bagInk: '#8a5a06',
  gearInk: '#0d7a63',
  blockInk: '#b3251a',
  allowInk: '#3f7a24',

  /* --- 도면 안의 판 ------------------------------------------------------
     판독 화면·배지·기록 카드처럼 도면 안에 놓이는 '화면' 이다.
     타일 색과 무관하게 언제나 밝다 — 종이 한 장이 놓인 것으로 읽혀야 한다. */
  panel: '#ffffff',
  panelSoft: '#f5f5f7',
  panelLine: '#d2d2d7',
  panelHair: '#e5e5ea',

  /* --- 그늘 · 장막 --------------------------------------------------------
     둘 다 '어둡게 만드는' 것이라 밝은 도면 위에서만 뜻이 선다.
     불투명도는 쓰는 쪽이 정한다. */
  shadow: '#000000', // 심볼 발치의 그늘 — .iso-shadow 와 같은 역할
  veil: '#9b91b3', // 관제가 닿지 않는 구역을 덮는 안개 — 국경 밖의 보랏빛 계열

  /* --- 판 위의 글자 ------------------------------------------------------ */
  ink: '#1d1d1f',
  ink2: '#6e6e73',
  ink3: '#8e8e93',
}
